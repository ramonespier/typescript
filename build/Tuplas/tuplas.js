const list = [];
list.push('Ramon');
const date = [30, 6, 2025];
// desestruturar tupla
function createDate(date) {
    const [day, month, year] = date;
}
createDate(date);
export {};
// createDate([30, 6, 2025])
